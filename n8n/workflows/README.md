# n8n/workflows/

Workflows N8n exportés au format JSON : `rappel-anniversaire.json`, `rappel-noel.json`,
`envoi-propositions.json`. Ils déclenchent le matching et l'envoi des e-mails de propositions
avant chaque événement (cf. section 8 du CLAUDE.md).

Responsable : **Agent Backend**.
