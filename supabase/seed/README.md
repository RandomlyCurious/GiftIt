# supabase/seed/

Données de départ insérées après les migrations : `tags.sql` (tags normalisés de référence)
et `catalogue.sql` (~200 produits de base). Uniquement des `INSERT`, jamais de modification de schéma.

Responsable : **Agent Data**. Interdit : modifier le schéma BDD.
