# supabase/functions/

Edge Functions Supabase (Deno / TypeScript). Contient la logique métier serverless du projet.
Pour l'instant une seule fonction : `matching/` (algorithme de scoring des produits).

Responsable : **Agent Backend**. Interdit : créer des composants React.
