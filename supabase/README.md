# supabase/

Racine de toute la configuration Supabase du projet (base de données, seed, Edge Functions).
Contient les migrations SQL, les données de seed (tags + catalogue) et les fonctions serverless.

Responsables : **Agent BDD** (migrations), **Agent Data** (seed), **Agent Backend** (functions).
