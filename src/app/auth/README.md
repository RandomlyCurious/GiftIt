# src/app/auth/

Pages d'authentification : connexion (login) et inscription (register), via Supabase Auth
(email + OAuth Google). Y placer les écrans et la gestion de session côté UI.

Responsable : **Agent Frontend**.
