# src/app/

Pages de l'application via le App Router de Next.js 14. Contient la landing (`page.tsx`)
et les routes : `auth/`, `dashboard/`, `proches/`, `swipe/`, `evenements/`.
Pas de logique métier dans les pages — elle va dans `src/lib/` ou les Edge Functions.

Responsable : **Agent Frontend**.
