# src/app/proches/

Routes liées aux proches : fiche d'un proche (`[id]/`) et formulaire d'ajout (`nouveau/`).
Gère l'affichage et la saisie des informations d'un proche (relation, date de naissance, tags...).

Responsable : **Agent Frontend**.
