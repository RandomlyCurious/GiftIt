# src/app/proches/nouveau/

Formulaire de création d'un nouveau proche (prénom, nom, date de naissance, relation,
adresse, photo, tags manuels initiaux). Insère ensuite dans la table `proches` via Supabase.

Responsable : **Agent Frontend**.
