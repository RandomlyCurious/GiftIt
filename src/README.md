# src/

Code source du frontend Next.js 14 (App Router, TypeScript). Regroupe les pages (`app/`),
les composants React réutilisables (`components/`), les clients et utilitaires (`lib/`)
et les types (`types/`).

Responsable : **Agent Frontend**. Interdit : migrations SQL ou logique métier hors composants.
