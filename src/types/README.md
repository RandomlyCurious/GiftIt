# src/types/

Définitions TypeScript du projet. Contient `database.types.ts`, types générés automatiquement
depuis le schéma Supabase (ne pas éditer à la main — régénérer après chaque migration).

Responsable : **Agent Frontend** (génération), schéma défini par l'**Agent BDD**.
